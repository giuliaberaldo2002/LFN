- generare il grafo (deadline 21/11)
    - our file has extension: .osm.pbf (binary file)
        - osmnx cannot handle it directly, we need to use pyrosm that directly read the binary file, we can't convert it into an xml
          because the dimension will be too big
         
    - osmnx.graph.graph_from_xml(param) -> to extract the graph: https://osmnx.readthedocs.io/en/stable/user-reference.html
    - return a multidigraph https://networkx.org/documentation/stable/reference/classes/multidigraph.html#networkx.MultiDiGraph
    - see other things
    - find a way to put the edges' tags into the node of which they are incident

- pruning: ()
    - compute the scores...for each node v \in V (23-24/11)
        - clustering coefficient https://networkx.org/documentation/stable/reference/algorithms/generated/networkx.algorithms.cluster.clustering.html#networkx.algorithms.cluster.clustering
        - betweeness centrality https://networkx.org/documentation/stable/reference/algorithms/generated/networkx.algorithms.centrality.betweenness_centrality.html#networkx.algorithms.centrality.betweenness_centrality
        - edges' length: 
                - for computing the neighbours: https://networkx.org/documentation/stable/reference/classes/generated/networkx.MultiDiGraph.adj.html#networkx.MultiDiGraph.adj
                - ask chat/gemini
        - tags: find some relevant tags:
                if(\exists tag == rural) score low (like 0 < score < 1)
                else if (\exists tag == city) score high (score > c) -> value of c?
                else (1 < score < c) # if the tag list does not contain significant tags
        - node degree: https://networkx.org/documentation/stable/reference/generated/networkx.classes.function.degree.html
        - others??
        - at the end create a vector of size = # of features and save it in an array (useful for applying DBSCAN later (point 2))
    
    1. determine the weights: (idea 23-24/11)
        - to determine!!

    - determine tau:
        - see
    
    2. unsupervised learning: DBSCAN https://scikit-learn.org/stable/modules/generated/sklearn.cluster.DBSCAN.html
        - normalize the features!!
        - prune all the nodes with label -1 (noise then rural)...and also other nodes? How?

- Clustering of the cities: (30/11)
    - to use Leiden, first install igraph library: https://python.igraph.org/en/stable/ -> see conversion
    - or see (with the related problems) https://networkx.org/documentation/stable/reference/algorithms/generated/networkx.algorithms.community.leiden.leiden_communities.html#networkx.algorithms.community.leiden.leiden_communities

- Sampling part: (12/12)
    1. First strategy:
        - label is_sampled = 0
        - sample k vertices such that:
            we have one vertex from each cluster, selected uniformly at random from all nodes with is_sampled = 0. 
            - If node v is sampled then is_sampled <- 1
        - if we have to extract other k vertices adopt the same strategy
        - if is_sampled == 1 for each v \in V then reset (is_sampled = 0), continue
    2. Second strategy:
        - apply FFT (implement it!) -> sequentially inefficient 
        - also here the mechanism with is_sampled labels is the same (but if a point with is_sampled = 1 is selected then return the 
          second farthest point , ecc..)



OPEN QUESTIONS:
- levels




-  



         
