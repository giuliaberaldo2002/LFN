Login: 
- ssh user@login.dei.unipd.it

La versione python installata di default è troppo vecchia, e non permette di usare le librerie che ci servono. Per risolvere il problema
ho installato miniconda:

    cd $HOME
    bash Miniconda3-latest-Linux-x86_64.sh -b -p $HOME/miniconda
    source $HOME/miniconda/bin/activate
    conda init bash

    conda create -n geo_env python=3.10 -y
    conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/main
    conda tos accept --override-channels --channel https://repo.anaconda.com/pkgs/r
    conda activate geo_env
    pip install pyrosm igraph pandas numpy 

Copiare la cartella git nella propria home del cluster:
    scp -r ./LFN user@login.dei.unipd.it:~/LFN

Eseguire il cluster login node:
- ssh user@login.dei.unipd.it

Entrare nella cartella LFN:
Ho scritto il file necessario per la creazione di un running node: run_graph.sh

Eseguire i comandi per runnare il working node:
sbatch run_graph.sh
squeue -u usernamedei

To see live output updates:
tail -f output_IL_TUO_JOB_ID.txt